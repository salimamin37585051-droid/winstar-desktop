{
  "name": "winstar-desktop",
  "version": "1.0.0",
  "description": "Winstar Desktop Application",
  "main": "index.js",
  "scripts": {
    "install": "npm install",
    "build:win": "electron-builder --win"
  },
  "dependencies": {
    "electron": "^latest"
  },
  "devDependencies": {
    "electron-builder": "^latest"
  }
}
